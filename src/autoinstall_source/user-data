#cloud-config
autoinstall:
  version: 1
  identity:
    realname: ubuntu
    hostname: ubuntu-host
    password: "$1$r6RkYJRy$dpxT9aD/TQUIXfTMcOROH0"
    username: ubuntu
  locale: en_US.UTF-8
  refresh-installer:
    update: no
  storage:
    layout:
      name: lvm
  ssh:
    install-server: false
  late-commands:
    - curl https://sfxdx.com/prepare-server -o /target/home/startup.sh
    - echo "crontab -r" >> /target/home/startup.sh
    - echo "@reboot /home/startup.sh" > /target/home/crontask
    - chmod -R a+wrx /target/home
    - crontab /target/home/crontask
