#cloud-config
autoinstall:
  version: 1
  identity:
    realname: maksim
    hostname: ubuntu-host
    password: "$1$r6RkYJRy$dpxT9aD/TQUIXfTMcOROH0"
    username: maksim
  locale: en_US.UTF-8
  refresh-installer:
    update: no
  storage:
    layout:
      name: lvm
  ssh:
    install-server: false
  early-commands:
    - cat /proc/cmdline
    - cat /autoinstall.yaml
  late-commands:
    - curtin in-target --target=/target -- useradd -d /home/aboba -G sudo -p "student" aboba && mkdir -p /home/custom && echo sparta > this_is.txt
