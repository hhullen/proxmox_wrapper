#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  identity:
    hostname: ubuntu-host
    password: '$y$j9T$1fDANh79eszOR.FUqB.0k/$ZsiHDWAreH4t98BX48H.BuMKzaELFDUPLt739wrE066'
    username: ubuntu
  refresh-installer:
    update: no
  storage:
    layout:
      name: lvm
  ssh:
    install-server: false
  early-commands:
    - cat /proc/cmdline
  late-commands:
    - curtin in-target --target=/target -- mkdir -p /home/custom && echo sparta > this_is.txt
