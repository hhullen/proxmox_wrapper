#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  identity:
    hostname: ubuntu-host
    password: '$1$rqzg0PyM$/XdQTMrMQ2vvSshV/lXvn.'
    username: ubuntu
  refresh-installer:
    update: no
  storage:
    layout:
      name: lvm
  ssh:
    install-server: false
  early-commands:
    - cat /proc/cmdline
    - cat /autoinstall.yaml
  late-commands:
    - curtin in-target --target=/target -- mkdir -p /home/custom && echo sparta > this_is.txt
